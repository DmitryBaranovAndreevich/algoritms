export enum ArrStyles {
  default = "",
  add = "add",
  swap = "swap",
  select = "select",
  finish = "finish",
  newEl = "newEl",
  addToIndex = "addToIndex",
  search = "search",
  delToIndex = "delToIndex",
  remove = "remove",
}
